name: Release Watchdog (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Watchdog is disabled
        run: echo "Release watchdog is disabled for now."
